var app;
(function (app) {
    var results;
    (function (results) {
        var IResults = /** @class */ (function () {
            function IResults() {
            }
            return IResults;
        }());
        results.IResults = IResults;
    })(results = app.results || (app.results = {}));
})(app || (app = {}));
